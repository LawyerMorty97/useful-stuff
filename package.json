{
  "name": "popspaper",
  "version": "1.0.0",
  "description": "",
  "main": "src/index.js",
  "scripts": {
    "clean": "rm -rf ./dist",
    "build:api": "npx tsc -p tsconfig.api.json",
    "build:functions": "npx tsc -p tsconfig.functions.json",
    "build": "npm run clean && npx concurrently \"npm run build:api\" \"npm run build:functions\"",
    "start:api": "node dist/api/index.js",
    "start:api:dev": "nodemon src/api/index.ts",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "dotenv": "^16.4.5",
    "express": "^4.21.0"
  },
  "devDependencies": {
    "@types/express": "^4.17.21",
    "@types/node": "^22.5.5",
    "typescript": "^5.6.2"
  }
}
